import { Subjects } from "./subjects";
interface ChargeCreatedEvent {
    subject: Subjects.ChargeCreate;
    data: {
        orderId: string;
        stripeId: string;
    };
}
export { ChargeCreatedEvent as chargeCreateEvent };
